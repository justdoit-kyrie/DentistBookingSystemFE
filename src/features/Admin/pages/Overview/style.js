import styled from 'styled-components';

export const DropdownWrapper = styled.div`
  .p-dropdown {
    background: transparent;
  }

  .p-dropdown-label {
    font-size: 1.3rem;
    font-weight: 500;
  }
`;
